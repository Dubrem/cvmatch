name: Keep Render app awake

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping cvmatch
        run: curl -sf --max-time 60 https://cvmatch-1b6e.onrender.com/api/health -o /dev/null
